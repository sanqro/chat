export interface IBigButton {
  children: string;
  destination: string;
}

export interface ITextInputField {
  placeholder?: string;
}

export interface ISearchResult {
  username: string;
  publicKey: string;
}
